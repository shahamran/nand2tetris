class shortfile { 
        field string hello;
        field int i,dont,care;
        
        function boolean foo(int a, int b, int c) {
                do foo(i,dont,care);
                let hello = "comment in a string // ";
                return;
        }
}
